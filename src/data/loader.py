"""Loads and normalizes IoT/ICS traffic datasets into a common feature schema.

TODO: implement loaders for at least one realistic IoT/ICS capture in addition
to a CICIDS/NSL-KDD baseline, so results can be compared across both.
"""


def load_dataset(name: str, split: str = "train"):
    raise NotImplementedError
